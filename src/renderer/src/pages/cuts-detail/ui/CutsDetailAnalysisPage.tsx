import { font, lightTheme } from "@heddy/design-tokens";

export const CutsDetailAnalysisPage = () => {
  return (
    <p className={`flex flex-1 items-center justify-center ${font.body.regular}`} style={{ color: lightTheme.label.assistive }}>
      분석완료 탭 준비 중
    </p>
  );
};
